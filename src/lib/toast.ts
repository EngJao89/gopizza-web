/**
 * Toasts globais — use em qualquer Client Component:
 * import { toast } from "@/lib/toast";
 */
export { toast, ToastContainer } from "react-toastify";
export type { ToastOptions, Id } from "react-toastify";
